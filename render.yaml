services:
  - type: web
    name: neemseed
    runtime: static
    repo: https://github.com/nbaldwin09/neemseed
    branch: main
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
